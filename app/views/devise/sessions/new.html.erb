<!--
  <%= bootstrap_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="container">


      <div class="field">
        <%= f.email_field :email, autofocus: true %>
      </div>

      <div class="field">
        <%= f.password_field :password, autocomplete: "off" %>
      </div>

      <div class="actions">
        <%= f.submit "Log in" %>
      </div>
    <% end %>

    <%= render "devise/shared/links" %>
</div> -->

<!-- This is the styled Form -->
    <body data-smooth-scroll-offset="77">

		<div class="nav-container">
      <div class="background-image-holder"><img alt="background" src="image/inner-6.jpg"></div>
		</div>
		<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
		<div class="main-container">
		<section class="height-100 imagebg text-center" data-overlay="4">
		    <div class="background-image-holder"><img alt="background" src="img/inner-6.jpg"></div>
		    <div class="container pos-vertical-center">
		        <div class="row">
		            <div class="col-sm-7 col-md-5">
		                <h2>Login to continue</h2>
		                <p class="lead">
		                    Welcome back, sign in with your existing Alloc8 account credentials
		                </p>
		                <form>
		                    <div class="row">
		                        <div class="col-sm-12">

		                            <input type="text" placeholder="Username" <%= f.email_field :email, autofocus: true %>
		                        </div>
		                        <div class="col-sm-12">

		                            <input type="password" placeholder="Password" <%= f.password_field :password, autocomplete: "off" %>
		                        </div>
		                        <div class="col-sm-12">
		                            <button class="btn btn--primary type--uppercase" type="submit" <%= f.submit "Log in" %> Login</button>
		                        </div>
		                    </div>
		                </form>
		                <span class="type--fine-print block">Dont have an account yet?
                      <%= link_to('Register', new_user_registration_path)  %>
                    </span>
		                <span class="type--fine-print block">Forgot your username or password?
                      <%= link_to "Forgot your password?", new_password_path(resource_name) %>
                    </span>
		            </div>
		        </div>
		    </div>
        <% end %>
        <%= render "devise/shared/links" %>
		</section></div>
